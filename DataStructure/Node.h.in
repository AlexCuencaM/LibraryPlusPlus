#ifndef NODE_HPP
#define NODE_HPP
template <typename T>
class Node
{
public:
    T Data;
    Node* left;
    Node* right;
    Node(T value);
    ~Node();
};

template <typename T>
Node<T>::Node(T value): Data(value), left(nullptr), right(nullptr)
{
}
template <typename T>
Node<T>::~Node()
{
}

#endif


