#ifndef USER_HPP
#define USER_HPP
#include <string>
using namespace std;
class User
{
    public:
        string Name;
        string Cedula;
        friend ostream& operator<<(ostream& os, const User& user);
        bool operator==(const User& other) const;
};
#endif