#ifndef BOOKS_VIEW_HPP
#define BOOKS_VIEW_HPP
#include <Services/BookService.hpp>
class BooksView
{
    private:
        /* data */
        BookService* bookService; // Pointer to BookService instance
        void SearchBook(std::string& title, std::string& author);
    public:
        BooksView(BookService& bookService);
        void ShowBooks();
        void InsertBook(Book &newBook);
        void ShowResultsBySearch();
        ~BooksView();
};



#endif // BOOKS_VIEW_HPP